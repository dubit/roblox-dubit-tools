local ReplicatedStorage = game:GetService("ReplicatedStorage")

local SequenceAnimator = require(ReplicatedStorage.Packages.SequenceAnimator)

SequenceAnimator.poseModelFromKeyframe(workspace.AvatarR5, workspace.Animation.Coolio, 2)
