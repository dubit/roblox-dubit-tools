[package]
name = "dubit/ugc-toolkit"
version = "0.0.1-dev.0"
realm = "shared"
registry = "https://github.com/Roblox-Devs-Dubit/wally-index"
licence = ""
authors = []

description = "the UGC Toolkit should be a suite/library of functions that enable developers to implement common functions that we normally assign the 3d podiums/UGCs in our experiences."

exclude = ["**"]
include = ["Source", "Source/*", "default.project.json", "wally.lock", "wally.toml"]

# [dev-dependencies]
# TestEz = "roblox/testez@0.4.1"

# [dependencies]
# Signal = "sleitnick/signal@2.0.3"